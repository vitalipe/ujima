# managed by the ujima image build — source: os/dev/audio-rig/snd-aloop.conf
#
# load the ALSA loopback module at boot — the dev audio rig's sink (see
# 99-ujima-dev-audio-rig.conf beside this)
snd-aloop
