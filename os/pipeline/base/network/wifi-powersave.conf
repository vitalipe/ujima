# Wifi powersave off, fleet-wide (2 = disable). Measured on the dev Pi
# 2026-08-08: DTIM dozing turns ~10ms LAN round trips into 40-100ms medians
# with outliers up to seconds, and saves at most ~0.2W — ujima machines are
# mains-powered, so responsiveness wins.
[connection]
wifi.powersave = 2
