#!/bin/sh
# managed by the ujima image build — source: os/pipeline/ujimaify/session/xinitrc
# UjimaOS — startx runs this; i3 (with the staged config) execs ujimad + the eww shell.
exec i3 -c /ujima/desktop/i3/config
