# managed by the ujima image build — source: os/ujimaify/journal/ujima.conf
#
# Persistent, capped journal — backed by the storage partition via slot->fstab's
# /var/log/journal bind. Storage=persistent is explicit: `auto` can stay volatile even
# with the dir present.
[Journal]
Storage=persistent
SystemMaxUse=256M
