;; Host-run stand-in for the image's env base layer — on device, env.edn is a cp of
;; /ujima/image.edn, staged by the ujimad script. Fake values, full stamp shape, so a
;; host run carries what a device carries; the API boundary decides what goes on the
;; wire (today: :version only).
{:version  "dev"
 :platform "rpi"
 :built-at "2026-01-01T00:00:00Z"
 :dev      true
 :base     {:url "dev" :sha256 "dev"}}
